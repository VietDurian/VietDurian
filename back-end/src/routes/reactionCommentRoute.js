import express from "express";

const Router = express.Router();

// Router.get('/', reactionCommentController.getAllReactionComments);
// Router.post('/', reactionCommentController.createReactionComment);
// Router.get('/:commentId/comment', reactionCommentController.getReactionCommentsByCommentId);
// Router.patch('/:id', reactionCommentController.updateReactionComment);
// Router.delete('/:id', reactionCommentController.deleteReactionComment);

export { Router as reactionCommentRoute };