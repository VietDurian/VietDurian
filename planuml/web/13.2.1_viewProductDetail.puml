@startuml 13.2.1_viewProductDetail
hide footbox
title View Product Detail

actor User as "Users"
boundary ProductDetailPage as View
participant ":ProductRoute" as Route
control "AuthMiddleware" as Middleware
control "ProductController" as Controller
participant ":ProductService" as Service
entity "ProductModel" as Model
database Database as Database

User -> View : 1. Open product detail page
activate View
activate User

    View -> Route : 2. GET /products/{id}
    activate Route

    Route -> Middleware : 3. AuthenticateUser(req, res, next)
    activate Middleware
    ref over User, Database : Verifies token
    Middleware -> Controller : 4. Forward ViewProductDetailRequest(productId)
    deactivate Middleware
    activate Controller

    Controller -> Service : 5. Process product detail retrieval
    activate Service

alt Product not found
    Service -> Model : 6.1. Find product by ID
    activate Model

    Model -> Database : 6.1.1. Retrieve product data by ID
    activate Database
    Database --> Model : 6.1.2. Result (null)
    deactivate Database

    Model --> Service : 6.1.3. Return not found
    deactivate Model

        Service --> Controller : 6.1.4. Return not found result
        Controller --> Route : 6.1.5. Return 404 Not Found + message
        Route --> View : 6.1.6. Show "Product not found"
else Product found
        Service -> Model : 6.2. Find product by ID
        activate Model

        Model -> Database : 6.2.1. Retrieve product data by ID
        activate Database
        Database --> Model : 6.2.2. Product data
        deactivate Database

        Model --> Service : 6.2.3. Return product entity
        deactivate Model

        Service --> Controller : 6.2.4. Return ProductDetailResponse
        deactivate Service
        Controller --> Route : 6.2.5. Return 200 OK + product data
        deactivate Controller
        Route --> View : 6.2.6. Render product detail
        deactivate Route
end

View --> User : 7. Display detail page content (detail or error)
deactivate View
deactivate User
@enduml
